from distutils.core import setup

from setuptools import find_packages

setup(
    name='korean_conjugator',
    version='0.0.1',
    description='Conjugator of Korean verbs and adjectives.',
    packages=find_packages(),
    requires=[])
